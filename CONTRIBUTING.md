# 기여 가이드

## git 작업 규칙

1. **작업 단위를 잘게 나눠서** 커밋하고 푸시한다. 하나의 커밋은 하나의 논리적 변경만 담는다.
2. 기능/특성에 따라 **GitHub 이슈를 먼저 만들고**, 그 이슈에 대응하는 **브랜치를 판 뒤** 해당 브랜치에서 작업한다.
   - `gh` CLI가 없는 환경에서는 [docs/이슈목록.md](docs/이슈목록.md)에 이슈를 먼저 기록하고, GitHub에도 동일하게 등록한다.
3. **브랜치명**: `타입/#이슈번호-설명` (예: `feat/#19-model-registry`)
   **커밋 제목**: `[타입] 한국어 설명` (예: `[feat] 모델 레지스트리 YAML 및 로더 추가`)
4. **커밋 본문은 육하원칙**(누가·언제·어디서·무엇을·어떻게·왜)에 따라 자세히 작성한다.
5. **모든 내용(문서·커밋·주석·로그 메시지)은 한국어로 작성한다.**

### 타입 키워드

| 타입 | 용도 |
|------|------|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `docs` | 문서만 변경 |
| `refactor` | 동작 변경 없는 구조 개선 |
| `test` | 테스트 추가/수정 |
| `chore` | 빌드·설정·의존성 등 잡무 |

### 커밋 본문 예시

```
[feat] 하드웨어 감지 및 모델 추천 명령(doctor) 추가

- 누가/언제: apg0001, 2026-08-31, Phase 2 진행 중
- 어디서: src/gigachanie/providers/hardware.py, cli.py
- 무엇을: OS/CPU/RAM/GPU를 감지하고 모델 레지스트리와 대조해
  실행 가능한 모델을 표로 출력하는 `giga doctor` 명령 구현
- 어떻게: psutil로 메모리·코어 수집, 플랫폼별 분기로 GPU VRAM 조회,
  KV 캐시 크기를 계산해 안전 컨텍스트 길이를 산출
- 왜: 사용자가 자기 장비에서 어떤 오픈모델을 돌릴 수 있는지
  직접 확인하고 선택할 수 있도록 하기 위함
```

## 개발 환경

```bash
python -m pip install -e ".[dev]"
ruff check .
mypy
pytest
```
