# 로드맵

## 단계

| Phase | 목표 | 상태 |
|-------|------|------|
| 0 | 베이스라인 체험 (Aider + Ollama), 모델 선정 | 진행 예정 |
| 1 | 서빙(#4)·에이전트 루프(#5)·쓰기도구+승인(#6)·편집엔진(#7) 완료 | **완료** |
| 2 | `doctor` 하드웨어 감지 + 모델 레지스트리 + 추천 | **완료** (이슈 #3) |
| 3 | repo map (tree-sitter) + 컨텍스트 예산 provider | 대기 |
| 4 | 메모리 하네스 1~3층 (컨텍스트파일·장기메모리·세션압축) | **완료** |
| 5 | 평가 하네스 (`giga eval`, 내장 태스크 3종) 완료. 태스크셋 확장·프롬프트 튜닝 진행 | 진행 중 |
| 6 | 승인 모드·체크포인트(undo) 완료. 셸 샌드박스·경로 권한 규칙 남음 | 진행 중 |
| 7 | 멀티 모델 오케스트레이션 (라우터/앙상블/분할/파이프라인) | 대기 |
| 8 | GUI — VS Code 확장 또는 독립 앱 | 대기 |

## CLI 완성 기준 (Phase 1~7)

- [ ] 로컬 모델(Ollama)에 연결해 실제 리포에서 "버그 고쳐줘" 수행
- [ ] `giga doctor` 로 장비별 모델 추천 + `giga model use` 로 선택
- [ ] repo map 자동 주입, 컨텍스트 초과 없이 대형 리포 대응
- [ ] AGENTS.md + 장기 메모리 로드/기록
- [ ] 셸 실행 승인 프롬프트, git 기반 undo
- [ ] 멀티 모델 모드 최소 1종(라우터) 동작
- [ ] eval 태스크셋 통과율 리포트

## 현재 스택

- Python 3.11+, typer + rich, psutil, pyyaml, httpx
- 서빙: Ollama 우선, 이후 llama.cpp / MLX / vLLM / 호스팅 API (OpenAI 호환 공용 어댑터)
